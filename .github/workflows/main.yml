name: Auto-update CHEO project ranking leaderboards

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup Node.js
      uses: actions/setup-node@v1
      with:
        node-version: '14.x'
    - uses: request/request-promise
    - name: Update overallranking.html page
      run: |
        npm install request-promise --save
        node update-rankings.js > overallranking.html
    - name: Commit changes & deploy
      run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -am "bot: Update CHEO project ranking leaderboards"
          git push
      env:
        CI: true
